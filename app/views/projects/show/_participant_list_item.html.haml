%li
  .project-participant-header
    .project-participant-name
      %div        
        Collaborating Organisation
      %div
        = link_to resource_path_from_uri(participant.uri) do
          = participant.label

  .project-participant-main
    = render 'application/grant_summary_stack', cost: project.offer_cost_sum_for_organization(participant), max_cost: project.offer_cost_sum, grant: project.offer_grant_sum_for_organization(participant), payments: 0 #project.payments_to_date_sum_for_organization(participant)

